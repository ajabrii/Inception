FROM alpine:3.13

RUN	apk update && apk upgrade && apk add --no-cache \
        vsftpd

COPY conf/vsftpd.conf /tmp/vsftpd.conf

COPY tools/init_ftp.sh /tmp/init_ftp.sh
ENTRYPOINT ["sh", "/tmp/init_ftp.sh"]
